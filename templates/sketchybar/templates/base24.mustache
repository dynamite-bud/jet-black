-- {{ scheme-name }} — SketchyBar colors (0xAARRGGBB)
-- Scheme author: {{ scheme-author }}
-- Drop-in replacement for colors.lua. NOTE: this is an OPAQUE jet-black bar —
-- it replaces a translucent/liquid-glass design if you have one.
local colors = {}

colors.white       = 0xff{{ base07-hex }}
colors.transparent = 0x00000000
colors.red         = 0xff{{ base08-hex }}
colors.orange      = 0xff{{ base09-hex }}
colors.charging    = 0xff{{ base0A-hex }}

colors.bar_color         = 0xff{{ base00-hex }}
colors.accent_color      = 0xff{{ base0D-hex }}
colors.secondary_accent  = 0xff{{ base0C-hex }}
colors.disabled_color    = 0xff{{ base04-hex }}
colors.background        = 0xff{{ base01-hex }}
colors.background_border = 0xff{{ base02-hex }}
colors.popup_background  = 0xee{{ base00-hex }}
colors.popup_border      = 0xff{{ base03-hex }}

return colors
